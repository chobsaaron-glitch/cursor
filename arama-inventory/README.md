# А-рама — склад багетных реек

Мобильное приложение для инвентаризации багетных реек в мастерской **«А-рама»**.

Работает как **PWA**: ставится на Android из Chrome и открывается как обычное приложение. Данные хранятся на устройстве (IndexedDB) и доступны офлайн.

## Возможности

- Учёт реек по **ячейкам склада**
- Поля: **артикул**, **длина (см)**, **количество**, **дата внесения**, **комментарий**, **фото по ссылке**
- Быстрый поиск и фильтр по ячейке
- Корректировка остатков (+/−) при инвентаризации
- Экспорт / импорт JSON для резервной копии

## Установка на Android

1. Соберите и опубликуйте приложение на HTTPS (или откройте локально через `npm run preview` в той же сети — для полноценной установки нужен HTTPS).
2. На телефоне откройте сайт в **Chrome**.
3. Меню **⋮** → **«Установить приложение»** / **«Добавить на главный экран»**.
4. Ярлык «А-рама» появится среди приложений.

Для разработки без сервера:

```bash
cd arama-inventory
npm install
npm run dev
```

Сборка:

```bash
npm run build
npm run preview
```

## Как пользоваться

1. **Добавить рейку** — кнопка «+ Добавить рейку».
2. Укажите номер ячейки, артикул, длину в см, количество, ссылку на фото с сайта и комментарий.
3. В карточке рейки меняйте остаток кнопками **+** / **−**.
4. В настройках (шестерёнка) сделайте **экспорт** перед сменой телефона.

## Стек

- React + TypeScript + Vite
- IndexedDB (`idb`)
- `vite-plugin-pwa` (manifest + service worker)
